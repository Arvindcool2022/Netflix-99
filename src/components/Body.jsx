import Hero from './Hero';
import ContentContainer from './ContentContainer';

const Body = () => {
  return (
    <div>
      <Hero />
      <ContentContainer />
    </div>
  );
};

export default Body;
